[project]
name = "meute-tui"
version = "0.1.0"
description = "Triage inbox for the meute fleet: terminal and, via textual-serve, browser."
requires-python = ">=3.11"
dependencies = ["textual>=8.0", "textual-serve>=1.1"]

[tool.uv]
package = false
